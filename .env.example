# Free OpenWeatherMap API key (https://openweathermap.org/api) for the Map view's Temperature
# overlay. Optional — without it, the Temperature overlay option is simply not offered.
VITE_OPENWEATHERMAP_API_KEY=
