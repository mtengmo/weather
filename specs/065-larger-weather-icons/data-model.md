# Phase 1 Data Model: Larger Weather Icons

No entities, no state, no persisted or transmitted data — this is a pure display-size change to
four existing `<img>`/`<Icon>` elements and one CSS rule. Produced for template consistency only;
there is nothing to model.
